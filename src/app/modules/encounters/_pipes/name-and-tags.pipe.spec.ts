import { NameAndTagsPipe } from './name-and-tags.pipe';

describe('NameAndTagsPipe', () => {
  it('create an instance', () => {
    const pipe = new NameAndTagsPipe();
    expect(pipe).toBeTruthy();
  });
});
